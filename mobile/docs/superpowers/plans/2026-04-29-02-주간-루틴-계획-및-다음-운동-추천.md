# 주간 루틴 계획 및 다음 운동 추천 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 사용자가 주간 루틴 세션 템플릿을 저장하고, 이번 주 완료 상태를 기준으로 다음 운동 세션을 추천해 운동 시작 시 기본 선택으로 반영한다.

**Architecture:** 이 계획은 `2026-04-29-01-부위-세분화.md` 완료 후 실행한다. 주간 루틴 설정은 `entities/workout-session/model`에 AsyncStorage 저장소와 타입을 두고, 완료 세션 매칭/추천 계산은 `entities/workout-session/lib`의 순수 함수로 둔다. Summary 표시는 `features/view-summary`, 운동 시작 시 자동 선택은 `features/start-workout` 훅과 `WorkoutContext` 액션으로 연결한다.

**Tech Stack:** Expo 55, React Native, TypeScript, NativeWind, i18next, AsyncStorage, bun

**Priority:** 2순위 / 부위 세분화 이후 실행

**Original Phase:** 2 + 3 merged

---

## 전제 조건

- `WorkoutBodyPartSet.details?: BodyPartDetail[]`가 존재한다.
- `BODY_PART_DETAILS`, `BodyPartDetail`, `toggleBodyPartDetail`이 export되어 있다.
- `BodyPartSelector`는 여러 상위 부위를 선택할 수 있고, long press로 세부 부위를 선택할 수 있다.

---

## 파일 맵

| 작업 | 경로 | Original Phase |
|------|------|----------------|
| Create | `src/entities/workout-session/model/weeklyRoutine.ts` | 2, 3 |
| Create | `src/entities/workout-session/model/weeklyRoutineStorage.ts` | 2 |
| Create | `src/entities/workout-session/model/__tests__/weeklyRoutineStorage.test.ts` | 2 |
| Create | `src/entities/workout-session/lib/weeklyRoutineProgress.ts` | 2, 3 |
| Create | `src/entities/workout-session/lib/__tests__/weeklyRoutineProgress.test.ts` | 2, 3 |
| Modify | `src/entities/workout-session/model/workoutState.ts` | 3 |
| Modify | `src/entities/workout-session/model/WorkoutContext.tsx` | 3 |
| Modify | `src/entities/workout-session/index.ts` | 2, 3 |
| Create | `src/features/view-summary/model/useWeeklyRoutinePlan.ts` | 2 |
| Create | `src/features/view-summary/ui/WeeklyRoutineSessionCard.tsx` | 2 |
| Create | `src/features/view-summary/ui/WeeklyRoutineList.tsx` | 2 |
| Create | `src/features/view-summary/ui/WeeklyRoutineSettingsSheet.tsx` | 2 |
| Modify | `src/features/view-summary/ui/SummaryScreen.tsx` | 2 |
| Modify | `src/features/view-summary/index.ts` | 2 |
| Modify | `src/shared/i18n/locales/ko.json` | 2 |
| Modify | `src/shared/i18n/locales/en.json` | 2 |
| Create | `src/features/start-workout/model/useNextRoutineSuggestion.ts` | 3 |
| Modify | `src/features/start-workout/ui/CountdownScreen.tsx` | 3 |
| Modify | `src/features/start-workout/index.ts` | 3 |

---

## 구현 계획

### Task 7: 주간 루틴 타입 + 저장소

**Files:**
- Create: `src/entities/workout-session/model/weeklyRoutine.ts`
- Create: `src/entities/workout-session/model/weeklyRoutineStorage.ts`
- Create: `src/entities/workout-session/model/__tests__/weeklyRoutineStorage.test.ts`

- [ ] **Step 1: 테스트 작성**

```typescript
// src/entities/workout-session/model/__tests__/weeklyRoutineStorage.test.ts
import AsyncStorage from "@react-native-async-storage/async-storage"
import {
  loadWeeklyRoutineSettings,
  saveWeeklyRoutineSettings,
} from "../weeklyRoutineStorage"
import type { WeeklyRoutineSettings } from "../weeklyRoutine"

jest.mock("@react-native-async-storage/async-storage", () =>
  require("@react-native-async-storage/async-storage/jest/async-storage-mock")
)

beforeEach(async () => {
  await AsyncStorage.clear()
})

test("returns null when nothing saved", async () => {
  expect(await loadWeeklyRoutineSettings()).toBeNull()
})

test("round-trips routine sessions with multiple parts and details", async () => {
  const settings: WeeklyRoutineSettings = {
    sessions: [
      {
        id: "arms-shoulders",
        parts: [
          { part: "arms", details: ["biceps", "triceps"] },
          { part: "shoulders", details: ["front_delts"] },
        ],
      },
    ],
  }

  await saveWeeklyRoutineSettings(settings)

  expect(await loadWeeklyRoutineSettings()).toEqual(settings)
})

test("overwrites previous routine", async () => {
  await saveWeeklyRoutineSettings({
    sessions: [{ id: "chest", parts: [{ part: "chest" }] }],
  })
  await saveWeeklyRoutineSettings({
    sessions: [{ id: "legs", parts: [{ part: "legs" }] }],
  })

  expect((await loadWeeklyRoutineSettings())?.sessions[0].id).toBe("legs")
})
```

- [ ] **Step 2: 테스트 실행 (실패 확인)**

```bash
cd mobile && bun test src/entities/workout-session/model/__tests__/weeklyRoutineStorage.test.ts
```

Expected: `weeklyRoutineStorage` module not found

- [ ] **Step 3: 타입 파일 구현**

```typescript
// src/entities/workout-session/model/weeklyRoutine.ts
import type { BodyPart, BodyPartDetail } from "./types"

export interface RoutinePart {
  part: BodyPart
  details?: BodyPartDetail[]
}

export interface WeeklyRoutineSession {
  id: string
  parts: RoutinePart[]
}

export interface WeeklyRoutineSettings {
  sessions: WeeklyRoutineSession[]
}

export const DEFAULT_WEEKLY_ROUTINE_SESSIONS: WeeklyRoutineSession[] = [
  { id: "chest", parts: [{ part: "chest" }] },
  { id: "back", parts: [{ part: "back" }] },
  { id: "legs", parts: [{ part: "legs" }] },
  {
    id: "arms-shoulders",
    parts: [{ part: "arms" }, { part: "shoulders" }],
  },
]
```

- [ ] **Step 4: 저장소 구현**

```typescript
// src/entities/workout-session/model/weeklyRoutineStorage.ts
import AsyncStorage from "@react-native-async-storage/async-storage"
import type { WeeklyRoutineSettings } from "./weeklyRoutine"

const KEY = "yb:workout:weekly-routine"

export async function saveWeeklyRoutineSettings(
  settings: WeeklyRoutineSettings,
): Promise<void> {
  await AsyncStorage.setItem(KEY, JSON.stringify(settings))
}

export async function loadWeeklyRoutineSettings(): Promise<WeeklyRoutineSettings | null> {
  const raw = await AsyncStorage.getItem(KEY)
  return raw ? (JSON.parse(raw) as WeeklyRoutineSettings) : null
}
```

- [ ] **Step 5: 테스트 통과 확인**

```bash
cd mobile && bun test src/entities/workout-session/model/__tests__/weeklyRoutineStorage.test.ts
```

Expected: 3개 PASS

- [ ] **Step 6: 커밋**

```bash
git add src/entities/workout-session/model/weeklyRoutine.ts src/entities/workout-session/model/weeklyRoutineStorage.ts src/entities/workout-session/model/__tests__/weeklyRoutineStorage.test.ts
git commit -m "feat(entities): add weekly routine settings storage"
```

---

### Task 8: 루틴 진행률/추천 순수 로직

**Files:**
- Create: `src/entities/workout-session/lib/weeklyRoutineProgress.ts`
- Create: `src/entities/workout-session/lib/__tests__/weeklyRoutineProgress.test.ts`

- [ ] **Step 1: 테스트 작성**

```typescript
// src/entities/workout-session/lib/__tests__/weeklyRoutineProgress.test.ts
import {
  buildWeeklyRoutineProgress,
  getNextRoutineSuggestion,
  isSessionMatchingRoutineSession,
} from "../weeklyRoutineProgress"
import type { StoredWorkoutSession } from "../../model/types"
import type { WeeklyRoutineSession } from "../../model/weeklyRoutine"

const makeSession = (
  id: string,
  bodyParts: StoredWorkoutSession["bodyParts"],
): StoredWorkoutSession => ({
  sessionId: id,
  startedAt: new Date().toISOString(),
  completedAt: new Date().toISOString(),
  bodyParts,
  memo: "",
  location: null,
})

const armsShoulders: WeeklyRoutineSession = {
  id: "arms-shoulders",
  parts: [{ part: "arms" }, { part: "shoulders" }],
}

test("matches a single-part routine by body part", () => {
  expect(
    isSessionMatchingRoutineSession(
      makeSession("s1", [{ part: "chest", setCount: 10 }]),
      { id: "chest", parts: [{ part: "chest" }] },
    ),
  ).toBe(true)
})

test("does not complete arms/shoulders unless both parts are in the same session", () => {
  expect(
    isSessionMatchingRoutineSession(
      makeSession("s1", [{ part: "arms", setCount: 10 }]),
      armsShoulders,
    ),
  ).toBe(false)
})

test("completes arms/shoulders when both parts are in the same session", () => {
  expect(
    isSessionMatchingRoutineSession(
      makeSession("s1", [
        { part: "arms", setCount: 8 },
        { part: "shoulders", setCount: 8 },
      ]),
      armsShoulders,
    ),
  ).toBe(true)
})

test("requires configured details when a routine part has details", () => {
  const routine: WeeklyRoutineSession = {
    id: "arms-detail",
    parts: [{ part: "arms", details: ["biceps", "triceps"] }],
  }

  expect(
    isSessionMatchingRoutineSession(
      makeSession("s1", [
        { part: "arms", details: ["biceps"], setCount: 8 },
      ]),
      routine,
    ),
  ).toBe(false)

  expect(
    isSessionMatchingRoutineSession(
      makeSession("s2", [
        { part: "arms", details: ["biceps", "triceps"], setCount: 8 },
      ]),
      routine,
    ),
  ).toBe(true)
})

test("builds progress with completed, partial, and pending routine sessions", () => {
  const routine: WeeklyRoutineSession[] = [
    { id: "chest", parts: [{ part: "chest" }] },
    armsShoulders,
    { id: "legs", parts: [{ part: "legs" }] },
  ]
  const sessions = [
    makeSession("s1", [{ part: "chest", setCount: 10 }]),
    makeSession("s2", [{ part: "arms", setCount: 10 }]),
  ]

  const progress = buildWeeklyRoutineProgress(routine, sessions)

  expect(progress.slots.map((slot) => slot.status)).toEqual([
    "completed",
    "partial",
    "pending",
  ])
  expect(progress.completedSessions).toBe(1)
  expect(progress.remainingSessions).toBe(2)
})

test("suggests the first incomplete routine session", () => {
  const progress = buildWeeklyRoutineProgress(
    [
      { id: "chest", parts: [{ part: "chest" }] },
      armsShoulders,
      { id: "legs", parts: [{ part: "legs" }] },
    ],
    [makeSession("s1", [{ part: "chest", setCount: 10 }])],
  )

  expect(getNextRoutineSuggestion(progress)?.id).toBe("arms-shoulders")
})

test("returns null when all routine sessions are completed", () => {
  const progress = buildWeeklyRoutineProgress(
    [{ id: "chest", parts: [{ part: "chest" }] }],
    [makeSession("s1", [{ part: "chest", setCount: 10 }])],
  )

  expect(getNextRoutineSuggestion(progress)).toBeNull()
})
```

- [ ] **Step 2: 테스트 실행 (실패 확인)**

```bash
cd mobile && bun test src/entities/workout-session/lib/__tests__/weeklyRoutineProgress.test.ts
```

Expected: `weeklyRoutineProgress` module not found

- [ ] **Step 3: 구현**

```typescript
// src/entities/workout-session/lib/weeklyRoutineProgress.ts
import type { StoredWorkoutSession, WorkoutBodyPartSet } from "../model/types"
import type { RoutinePart, WeeklyRoutineSession } from "../model/weeklyRoutine"

export type WeeklyRoutineSlotStatus = "pending" | "partial" | "completed"

export interface WeeklyRoutineSlotProgress {
  index: number
  routineSession: WeeklyRoutineSession
  matchedSession: StoredWorkoutSession | null
  status: WeeklyRoutineSlotStatus
}

export interface WeeklyRoutineProgress {
  totalSessions: number
  completedSessions: number
  remainingSessions: number
  slots: WeeklyRoutineSlotProgress[]
}

function detailsMatch(required: RoutinePart, completed: WorkoutBodyPartSet) {
  if (!required.details || required.details.length === 0) {
    return true
  }

  const completedDetails = completed.details ?? []
  return required.details.every((detail) => completedDetails.includes(detail))
}

function partMatches(required: RoutinePart, completed: WorkoutBodyPartSet) {
  return required.part === completed.part && detailsMatch(required, completed)
}

function partOverlaps(required: RoutinePart, completed: WorkoutBodyPartSet) {
  return required.part === completed.part
}

export function isSessionMatchingRoutineSession(
  session: StoredWorkoutSession,
  routineSession: WeeklyRoutineSession,
) {
  return routineSession.parts.every((required) =>
    session.bodyParts.some((completed) => partMatches(required, completed)),
  )
}

function isSessionPartiallyMatchingRoutineSession(
  session: StoredWorkoutSession,
  routineSession: WeeklyRoutineSession,
) {
  if (isSessionMatchingRoutineSession(session, routineSession)) {
    return false
  }

  return routineSession.parts.some((required) =>
    session.bodyParts.some((completed) => partOverlaps(required, completed)),
  )
}

export function buildWeeklyRoutineProgress(
  routineSessions: WeeklyRoutineSession[],
  weekSessions: StoredWorkoutSession[],
): WeeklyRoutineProgress {
  const usedCompletedSessionIds = new Set<string>()

  const slots = routineSessions.map<WeeklyRoutineSlotProgress>(
    (routineSession, index) => {
      const matchedSession = weekSessions.find(
        (session) =>
          !usedCompletedSessionIds.has(session.sessionId) &&
          isSessionMatchingRoutineSession(session, routineSession),
      )

      if (matchedSession) {
        usedCompletedSessionIds.add(matchedSession.sessionId)
        return {
          index,
          routineSession,
          matchedSession,
          status: "completed",
        }
      }

      const partialSession = weekSessions.find((session) =>
        isSessionPartiallyMatchingRoutineSession(session, routineSession),
      )

      return {
        index,
        routineSession,
        matchedSession: partialSession ?? null,
        status: partialSession ? "partial" : "pending",
      }
    },
  )

  const completedSessions = slots.filter(
    (slot) => slot.status === "completed",
  ).length

  return {
    totalSessions: routineSessions.length,
    completedSessions,
    remainingSessions: Math.max(0, routineSessions.length - completedSessions),
    slots,
  }
}

export function getNextRoutineSuggestion(
  progress: WeeklyRoutineProgress,
): WeeklyRoutineSession | null {
  return (
    progress.slots.find((slot) => slot.status !== "completed")
      ?.routineSession ?? null
  )
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
cd mobile && bun test src/entities/workout-session/lib/__tests__/weeklyRoutineProgress.test.ts
```

Expected: 7개 PASS

- [ ] **Step 5: 커밋**

```bash
git add src/entities/workout-session/lib/weeklyRoutineProgress.ts src/entities/workout-session/lib/__tests__/weeklyRoutineProgress.test.ts
git commit -m "feat(entities): add weekly routine progress matching"
```

---

### Task 9: WorkoutContext에 루틴 템플릿 적용 액션 추가

**Files:**
- Modify: `src/entities/workout-session/model/workoutState.ts`
- Modify: `src/entities/workout-session/model/WorkoutContext.tsx`
- Create: `src/entities/workout-session/model/__tests__/workoutStateRoutine.test.ts`

- [ ] **Step 1: 테스트 작성**

```typescript
// src/entities/workout-session/model/__tests__/workoutStateRoutine.test.ts
import { initialWorkoutState, workoutReducer } from "../workoutState"

test("APPLY_BODY_PART_TEMPLATE replaces selected body parts with default set counts", () => {
  const next = workoutReducer(
    {
      ...initialWorkoutState,
      bodyParts: [{ part: "chest", setCount: 10 }],
    },
    {
      type: "APPLY_BODY_PART_TEMPLATE",
      payload: [
        { part: "arms", details: ["biceps"] },
        { part: "shoulders", details: ["front_delts"] },
      ],
    },
  )

  expect(next.bodyParts).toEqual([
    { part: "arms", details: ["biceps"], setCount: 10 },
    { part: "shoulders", details: ["front_delts"], setCount: 10 },
  ])
})
```

- [ ] **Step 2: 테스트 실행 (실패 확인)**

```bash
cd mobile && bun test src/entities/workout-session/model/__tests__/workoutStateRoutine.test.ts
```

Expected: `APPLY_BODY_PART_TEMPLATE` action type error

- [ ] **Step 3: workoutState.ts 수정**

`src/entities/workout-session/model/workoutState.ts`의 import에 `RoutinePart`를 추가한다:

```typescript
import type { RoutinePart } from "./weeklyRoutine"
```

`WorkoutAction` 유니언에 추가한다:

```typescript
| { type: "APPLY_BODY_PART_TEMPLATE"; payload: RoutinePart[] }
```

`workoutReducer` switch에 추가한다:

```typescript
case "APPLY_BODY_PART_TEMPLATE":
  return {
    ...state,
    bodyParts: action.payload.map((item) => ({
      part: item.part,
      details: item.details,
      setCount: 10,
    })),
  }
```

- [ ] **Step 4: WorkoutContext.tsx에 액션 노출**

`WorkoutContextValue`에 추가한다:

```typescript
applyBodyPartTemplate: (parts: RoutinePart[]) => void
```

타입 import에 `RoutinePart`를 추가한다:

```typescript
import type { RoutinePart } from "./weeklyRoutine"
```

Provider 내부에 callback을 추가한다:

```typescript
const applyBodyPartTemplate = useCallback((parts: RoutinePart[]) => {
  dispatch({ type: "APPLY_BODY_PART_TEMPLATE", payload: parts })
}, [])
```

`value`와 dependency 배열에 `applyBodyPartTemplate`을 추가한다.

- [ ] **Step 5: 테스트와 TypeScript 확인**

```bash
cd mobile && bun test src/entities/workout-session/model/__tests__/workoutStateRoutine.test.ts && bunx tsc --noEmit
```

Expected: 테스트 PASS, TypeScript 에러 없음

- [ ] **Step 6: 커밋**

```bash
git add src/entities/workout-session/model/workoutState.ts src/entities/workout-session/model/WorkoutContext.tsx src/entities/workout-session/model/__tests__/workoutStateRoutine.test.ts
git commit -m "feat(workout): apply routine body part templates"
```

---

### Task 10: entities export 추가

**Files:**
- Modify: `src/entities/workout-session/index.ts`

- [ ] **Step 1: export 추가**

```typescript
export {
  DEFAULT_WEEKLY_ROUTINE_SESSIONS,
} from "./model/weeklyRoutine"
export type {
  RoutinePart,
  WeeklyRoutineSession,
  WeeklyRoutineSettings,
} from "./model/weeklyRoutine"
export {
  loadWeeklyRoutineSettings,
  saveWeeklyRoutineSettings,
} from "./model/weeklyRoutineStorage"
export {
  buildWeeklyRoutineProgress,
  getNextRoutineSuggestion,
  isSessionMatchingRoutineSession,
} from "./lib/weeklyRoutineProgress"
export type {
  WeeklyRoutineProgress,
  WeeklyRoutineSlotProgress,
  WeeklyRoutineSlotStatus,
} from "./lib/weeklyRoutineProgress"
```

- [ ] **Step 2: TypeScript 확인**

```bash
cd mobile && bunx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 3: 커밋**

```bash
git add src/entities/workout-session/index.ts
git commit -m "feat(entities): export weekly routine APIs"
```

---

### Task 11: useWeeklyRoutinePlan 훅 + i18n

**Files:**
- Create: `src/features/view-summary/model/useWeeklyRoutinePlan.ts`
- Modify: `src/shared/i18n/locales/ko.json`
- Modify: `src/shared/i18n/locales/en.json`

- [ ] **Step 1: 훅 구현**

```typescript
// src/features/view-summary/model/useWeeklyRoutinePlan.ts
import { useCallback, useEffect, useMemo, useState } from "react"
import { useTranslation } from "react-i18next"
import {
  buildWeeklyRoutineProgress,
  DEFAULT_WEEKLY_ROUTINE_SESSIONS,
  getStoredWorkoutSessionsInRange,
  loadWeeklyRoutineSettings,
  saveWeeklyRoutineSettings,
} from "@/entities/workout-session"
import type {
  StoredWorkoutSession,
  WeeklyRoutineSettings,
} from "@/entities/workout-session"
import { getThisWeekDateRange } from "@/shared/lib/date"

export function useWeeklyRoutinePlan() {
  const { t } = useTranslation()
  const [settings, setSettings] = useState<WeeklyRoutineSettings | null>(null)
  const [sessions, setSessions] = useState<StoredWorkoutSession[]>([])
  const [isLoading, setIsLoading] = useState(true)

  const load = useCallback(async () => {
    setIsLoading(true)
    const loadedSettings = await loadWeeklyRoutineSettings()
    const { startDateKey, endDateKey } = getThisWeekDateRange()
    const weekSessions = await getStoredWorkoutSessionsInRange(
      startDateKey,
      endDateKey,
    )
    setSettings(loadedSettings)
    setSessions(weekSessions)
    setIsLoading(false)
  }, [])

  useEffect(() => {
    void load()
  }, [load])

  const updateSettings = useCallback(async (next: WeeklyRoutineSettings) => {
    await saveWeeklyRoutineSettings(next)
    setSettings(next)
  }, [])

  const effectiveSessions =
    settings?.sessions ?? DEFAULT_WEEKLY_ROUTINE_SESSIONS

  const progress = useMemo(
    () => buildWeeklyRoutineProgress(effectiveSessions, sessions),
    [effectiveSessions, sessions],
  )

  return {
    settings,
    progress,
    isLoading,
    reload: load,
    updateSettings,
    hasCustomSettings: settings !== null,
    summaryText: t("workout.weeklyRoutine.summary", {
      completed: progress.completedSessions,
      total: progress.totalSessions,
    }),
    remainingText: t("workout.weeklyRoutine.remaining", {
      remaining: progress.remainingSessions,
    }),
  }
}
```

- [ ] **Step 2: ko.json 문자열 추가**

`src/shared/i18n/locales/ko.json`의 `workout` 객체에 추가한다:

```json
"weeklyRoutine": {
  "title": "주간 루틴",
  "summary": "이번 주 루틴 {{total}}개 중 {{completed}}개 완료",
  "remaining": "아직 {{remaining}}개 루틴이 남았어요",
  "settings": {
    "title": "주간 루틴 설정",
    "save": "저장",
    "placeholder": "이번 주 루틴을 설정하세요"
  },
  "status": {
    "completed": "완료",
    "partial": "부분",
    "pending": "예정"
  },
  "suggestion": {
    "reason": "이번 주 루틴에서 아직 완료하지 않은 세션이에요",
    "complete": "이번 주 루틴을 모두 완료했어요"
  }
}
```

- [ ] **Step 3: en.json 문자열 추가**

`src/shared/i18n/locales/en.json`의 `workout` 객체에 추가한다:

```json
"weeklyRoutine": {
  "title": "Weekly Routine",
  "summary": "{{completed}} of {{total}} routine sessions completed this week",
  "remaining": "{{remaining}} routine sessions remaining",
  "settings": {
    "title": "Weekly Routine Settings",
    "save": "Save",
    "placeholder": "Set this week's routine"
  },
  "status": {
    "completed": "Done",
    "partial": "Partial",
    "pending": "Planned"
  },
  "suggestion": {
    "reason": "This is an unfinished session from your weekly routine",
    "complete": "You completed this week's routine"
  }
}
```

- [ ] **Step 4: TypeScript 확인**

```bash
cd mobile && bunx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 5: 커밋**

```bash
git add src/features/view-summary/model/useWeeklyRoutinePlan.ts src/shared/i18n/locales/ko.json src/shared/i18n/locales/en.json
git commit -m "feat(view-summary): add weekly routine plan hook"
```

---

### Task 12: Summary에 주간 루틴 진행률 UI 연결

**Files:**
- Create: `src/features/view-summary/ui/WeeklyRoutineSessionCard.tsx`
- Create: `src/features/view-summary/ui/WeeklyRoutineList.tsx`
- Create: `src/features/view-summary/ui/WeeklyRoutineSettingsSheet.tsx`
- Modify: `src/features/view-summary/ui/SummaryScreen.tsx`
- Modify: `src/features/view-summary/index.ts`

- [ ] **Step 1: WeeklyRoutineSessionCard 구현**

```typescript
// src/features/view-summary/ui/WeeklyRoutineSessionCard.tsx
import { Text, View } from "react-native"
import { useTranslation } from "react-i18next"
import type { WeeklyRoutineSlotProgress } from "@/entities/workout-session"
import { bodyPartLabel } from "@/shared/lib/format"

interface WeeklyRoutineSessionCardProps {
  slot: WeeklyRoutineSlotProgress
}

export function WeeklyRoutineSessionCard({ slot }: WeeklyRoutineSessionCardProps) {
  const { t } = useTranslation()
  const label = slot.routineSession.parts
    .map((item) => bodyPartLabel(item.part))
    .join(" / ")
  const isCompleted = slot.status === "completed"
  const isPartial = slot.status === "partial"

  return (
    <View
      className={`min-w-[72px] rounded-yb-md border px-yb-3 py-yb-2 ${
        isCompleted
          ? "border-yb-accent bg-yb-fill-pale"
          : isPartial
            ? "border-yb-accent/60 bg-yb-surface/80"
            : "border-yb-border bg-yb-surface/60"
      }`}
    >
      <Text className="text-yb-caption font-semibold text-yb-fg">
        {slot.index + 1}
      </Text>
      <Text className="mt-yb-1 text-yb-caption text-yb-fg-secondary" numberOfLines={1}>
        {label}
      </Text>
      <Text className="mt-yb-1 text-[10px] text-yb-fg-tertiary">
        {t(`workout.weeklyRoutine.status.${slot.status}`)}
      </Text>
    </View>
  )
}
```

- [ ] **Step 2: WeeklyRoutineList 구현**

```typescript
// src/features/view-summary/ui/WeeklyRoutineList.tsx
import { Pressable, ScrollView, Text, View } from "react-native"
import { useTranslation } from "react-i18next"
import { Card } from "@/shared/ui/Card"
import { useWeeklyRoutinePlan } from "../model/useWeeklyRoutinePlan"
import { WeeklyRoutineSessionCard } from "./WeeklyRoutineSessionCard"

interface WeeklyRoutineListProps {
  onPressSettings?: () => void
  onLongPress?: () => void
}

export function WeeklyRoutineList({
  onPressSettings,
  onLongPress,
}: WeeklyRoutineListProps) {
  const { t } = useTranslation()
  const {
    progress,
    isLoading,
    hasCustomSettings,
    summaryText,
    remainingText,
  } = useWeeklyRoutinePlan()

  if (isLoading) return null

  return (
    <Pressable onLongPress={onLongPress} delayLongPress={450}>
      <Card variant="glass">
        <Card.Header
          label={t("workout.weeklyRoutine.title")}
          more={t("workout.weeklyRoutine.settings.title")}
          chevron
          onMorePress={onPressSettings}
        />
        <Card.Spacer size={12} />
        {!hasCustomSettings && (
          <Pressable onPress={onPressSettings} className="mb-yb-3">
            <Card.Caption>
              {t("workout.weeklyRoutine.settings.placeholder")}
            </Card.Caption>
          </Pressable>
        )}
        <Text className="text-yb-body-sm font-semibold text-yb-fg">
          {summaryText}
        </Text>
        <ScrollView
          horizontal
          showsHorizontalScrollIndicator={false}
          contentContainerClassName="mt-yb-3 flex-row gap-yb-2"
        >
          {progress.slots.map((slot) => (
            <WeeklyRoutineSessionCard key={slot.routineSession.id} slot={slot} />
          ))}
        </ScrollView>
        <Card.Spacer size={10} />
        <Card.Caption>{remainingText}</Card.Caption>
      </Card>
    </Pressable>
  )
}
```

- [ ] **Step 3: WeeklyRoutineSettingsSheet 구현**

4개 세션 슬롯은 고정하고, 각 슬롯 안에서 상위 부위와 세부 부위를 편집한다. `팔/어깨`처럼 여러 상위 부위를 한 슬롯에 넣으면 같은 운동 세션에서 함께 수행해야 완료 처리된다.

```typescript
// src/features/view-summary/ui/WeeklyRoutineSettingsSheet.tsx
import { useEffect, useState } from "react"
import { Modal, Pressable, ScrollView, Text, View } from "react-native"
import { useTranslation } from "react-i18next"
import {
  BODY_PART_DETAILS,
  DEFAULT_WEEKLY_ROUTINE_SESSIONS,
  type BodyPart,
  type BodyPartDetail,
  type RoutinePart,
  type WeeklyRoutineSettings,
} from "@/entities/workout-session"
import { bodyPartDetailLabel, bodyPartLabel } from "@/shared/lib/format"
import { useWeeklyRoutinePlan } from "../model/useWeeklyRoutinePlan"

const ALL_BODY_PARTS: BodyPart[] = [
  "chest",
  "back",
  "legs",
  "shoulders",
  "arms",
  "core",
]

interface WeeklyRoutineSettingsSheetProps {
  visible: boolean
  onClose: () => void
}

function toggleDetail(
  current: RoutinePart[],
  part: BodyPart,
  detail: BodyPartDetail,
) {
  return current.map((item) => {
    if (item.part !== part) return item
    const details = item.details ?? []
    return {
      ...item,
      details: details.includes(detail)
        ? details.filter((value) => value !== detail)
        : [...details, detail],
    }
  })
}

export function WeeklyRoutineSettingsSheet({
  visible,
  onClose,
}: WeeklyRoutineSettingsSheetProps) {
  const { t } = useTranslation()
  const { settings, updateSettings } = useWeeklyRoutinePlan()
  const [draft, setDraft] = useState<WeeklyRoutineSettings>({
    sessions: DEFAULT_WEEKLY_ROUTINE_SESSIONS,
  })

  useEffect(() => {
    if (visible) {
      setDraft({
        sessions: settings?.sessions ?? DEFAULT_WEEKLY_ROUTINE_SESSIONS,
      })
    }
  }, [settings, visible])

  const handleSave = async () => {
    await updateSettings(draft)
    onClose()
  }

  const updateSessionParts = (index: number, parts: RoutinePart[]) => {
    setDraft((current) => ({
      sessions: current.sessions.map((session, sessionIndex) =>
        sessionIndex === index ? { ...session, parts } : session,
      ),
    }))
  }

  const handleTogglePart = (index: number, part: BodyPart) => {
    const currentParts = draft.sessions[index]?.parts ?? []
    const exists = currentParts.some((item) => item.part === part)
    updateSessionParts(
      index,
      exists
        ? currentParts.filter((item) => item.part !== part)
        : [...currentParts, { part }],
    )
  }

  const handleToggleDetail = (
    index: number,
    part: BodyPart,
    detail: BodyPartDetail,
  ) => {
    const currentParts = draft.sessions[index]?.parts ?? []
    if (!currentParts.some((item) => item.part === part)) {
      return
    }
    updateSessionParts(index, toggleDetail(currentParts, part, detail))
  }

  return (
    <Modal visible={visible} transparent animationType="slide" onRequestClose={onClose}>
      <Pressable className="flex-1 justify-end" onPress={onClose}>
        <Pressable
          className="max-h-[86%] rounded-t-yb-xl bg-yb-surface px-yb-6 pb-yb-10 pt-yb-4"
          onPress={() => {}}
        >
          <View className="mb-yb-6 h-1 w-10 self-center rounded-full bg-yb-border" />
          <Text className="mb-yb-4 text-yb-title font-semibold text-yb-fg">
            {t("workout.weeklyRoutine.settings.title")}
          </Text>
          <ScrollView showsVerticalScrollIndicator={false} contentContainerClassName="gap-yb-4">
            {draft.sessions.map((session, index) => (
              <View
                key={session.id}
                className="rounded-yb-md border border-yb-border bg-yb-fill-pale px-yb-4 py-yb-3"
              >
                <Text className="mb-yb-3 text-yb-body-sm font-semibold text-yb-fg">
                  {index + 1}
                </Text>
                <View className="flex-row flex-wrap gap-yb-2">
                  {ALL_BODY_PARTS.map((part) => {
                    const active = session.parts.some((item) => item.part === part)
                    return (
                      <Pressable
                        key={part}
                        onPress={() => handleTogglePart(index, part)}
                        className={`rounded-yb-md border px-yb-3 py-yb-2 ${
                          active
                            ? "border-yb-accent bg-yb-accent"
                            : "border-yb-border bg-yb-surface"
                        }`}
                      >
                        <Text
                          className={`text-yb-caption font-semibold ${
                            active ? "text-white" : "text-yb-fg-secondary"
                          }`}
                        >
                          {bodyPartLabel(part)}
                        </Text>
                      </Pressable>
                    )
                  })}
                </View>
                {session.parts.map((routinePart) => {
                  const details = BODY_PART_DETAILS[routinePart.part]
                  if (details.length === 0) return null

                  return (
                    <View key={routinePart.part} className="mt-yb-3">
                      <Text className="mb-yb-2 text-[10px] font-semibold text-yb-fg-tertiary">
                        {bodyPartLabel(routinePart.part)}
                      </Text>
                      <View className="flex-row flex-wrap gap-yb-2">
                        {details.map((detail) => {
                          const active = routinePart.details?.includes(detail) ?? false
                          return (
                            <Pressable
                              key={detail}
                              onPress={() => handleToggleDetail(index, routinePart.part, detail)}
                              className={`rounded-full border px-yb-3 py-yb-1 ${
                                active
                                  ? "border-yb-accent bg-yb-accent"
                                  : "border-yb-border bg-yb-surface"
                              }`}
                            >
                              <Text
                                className={`text-[10px] ${
                                  active ? "text-white" : "text-yb-fg-secondary"
                                }`}
                              >
                                {bodyPartDetailLabel(detail)}
                              </Text>
                            </Pressable>
                          )
                        })}
                      </View>
                    </View>
                  )
                })}
              </View>
            ))}
          </ScrollView>
          <Pressable
            onPress={handleSave}
            className="mt-yb-6 items-center rounded-yb-md bg-yb-accent py-yb-3"
          >
            <Text className="font-semibold text-white">
              {t("workout.weeklyRoutine.settings.save")}
            </Text>
          </Pressable>
        </Pressable>
      </Pressable>
    </Modal>
  )
}
```

- [ ] **Step 4: SummaryScreen 연결**

`src/features/view-summary/ui/SummaryScreen.tsx`에 import와 state를 추가한다:

```typescript
import { WeeklyRoutineList } from "./WeeklyRoutineList"
import { WeeklyRoutineSettingsSheet } from "./WeeklyRoutineSettingsSheet"
```

컴포넌트 내부에 추가한다:

```typescript
const [isRoutineSettingsOpen, setIsRoutineSettingsOpen] = useState(false)
```

`renderSummaryCard`의 `weeklySessions` case를 기존 `WeeklySessionList` 대신 아래로 교체한다:

```typescript
case "weeklySessions":
  return (
    <WeeklyRoutineList
      onLongPress={enterEditMode}
      onPressSettings={() => setIsRoutineSettingsOpen(true)}
    />
  )
```

JSX 하단 `SummaryCardEditModal` 아래에 추가한다:

```typescript
<WeeklyRoutineSettingsSheet
  visible={isRoutineSettingsOpen}
  onClose={() => setIsRoutineSettingsOpen(false)}
/>
```

- [ ] **Step 5: feature index export 추가**

`src/features/view-summary/index.ts`에 추가한다:

```typescript
export { useWeeklyRoutinePlan } from "./model/useWeeklyRoutinePlan"
export { WeeklyRoutineList } from "./ui/WeeklyRoutineList"
export { WeeklyRoutineSettingsSheet } from "./ui/WeeklyRoutineSettingsSheet"
```

- [ ] **Step 6: TypeScript 확인**

```bash
cd mobile && bunx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 7: 커밋**

```bash
git add src/features/view-summary/model/useWeeklyRoutinePlan.ts src/features/view-summary/ui/WeeklyRoutineSessionCard.tsx src/features/view-summary/ui/WeeklyRoutineList.tsx src/features/view-summary/ui/WeeklyRoutineSettingsSheet.tsx src/features/view-summary/ui/SummaryScreen.tsx src/features/view-summary/index.ts
git commit -m "feat(view-summary): show weekly routine progress"
```

---

### Task 13: 다음 루틴 추천 훅 + 카운트다운 연결

**Files:**
- Create: `src/features/start-workout/model/useNextRoutineSuggestion.ts`
- Modify: `src/features/start-workout/ui/CountdownScreen.tsx`
- Modify: `src/features/start-workout/index.ts`

- [ ] **Step 1: 훅 구현**

```typescript
// src/features/start-workout/model/useNextRoutineSuggestion.ts
import { useEffect, useState } from "react"
import {
  buildWeeklyRoutineProgress,
  DEFAULT_WEEKLY_ROUTINE_SESSIONS,
  getNextRoutineSuggestion,
  getStoredWorkoutSessionsInRange,
  loadWeeklyRoutineSettings,
} from "@/entities/workout-session"
import type { WeeklyRoutineSession } from "@/entities/workout-session"
import { getThisWeekDateRange } from "@/shared/lib/date"

export function useNextRoutineSuggestion(): {
  suggestion: WeeklyRoutineSession | null
  isLoading: boolean
} {
  const [suggestion, setSuggestion] = useState<WeeklyRoutineSession | null>(null)
  const [isLoading, setIsLoading] = useState(true)

  useEffect(() => {
    let mounted = true

    async function load() {
      const settings = await loadWeeklyRoutineSettings()
      const { startDateKey, endDateKey } = getThisWeekDateRange()
      const weekSessions = await getStoredWorkoutSessionsInRange(
        startDateKey,
        endDateKey,
      )
      const routineSessions =
        settings?.sessions ?? DEFAULT_WEEKLY_ROUTINE_SESSIONS
      const progress = buildWeeklyRoutineProgress(routineSessions, weekSessions)

      if (mounted) {
        setSuggestion(getNextRoutineSuggestion(progress))
        setIsLoading(false)
      }
    }

    void load().catch(() => {
      if (mounted) {
        setSuggestion(null)
        setIsLoading(false)
      }
    })

    return () => {
      mounted = false
    }
  }, [])

  return { suggestion, isLoading }
}
```

- [ ] **Step 2: CountdownScreen에 자동 적용**

`src/features/start-workout/ui/CountdownScreen.tsx`에 import를 추가한다:

```typescript
import { useNextRoutineSuggestion } from "../model/useNextRoutineSuggestion"
```

`useWorkout()`에서 `state`, `applyBodyPartTemplate`을 함께 가져온다:

```typescript
const {
  state,
  setLocation,
  startCountdown,
  startRecording,
  applyBodyPartTemplate,
} = useWorkout()
```

훅을 호출한다:

```typescript
const { suggestion, isLoading: suggestionLoading } = useNextRoutineSuggestion()
```

`startCountdown()` effect 아래에 추가한다:

```typescript
useEffect(() => {
  if (suggestionLoading || !suggestion || state.bodyParts.length > 0) {
    return
  }

  applyBodyPartTemplate(suggestion.parts)
}, [
  applyBodyPartTemplate,
  state.bodyParts.length,
  suggestion,
  suggestionLoading,
])
```

- [ ] **Step 3: feature index export 추가**

`src/features/start-workout/index.ts`에 추가한다:

```typescript
export { useNextRoutineSuggestion } from "./model/useNextRoutineSuggestion"
```

- [ ] **Step 4: TypeScript 확인**

```bash
cd mobile && bunx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 5: 커밋**

```bash
git add src/features/start-workout/model/useNextRoutineSuggestion.ts src/features/start-workout/ui/CountdownScreen.tsx src/features/start-workout/index.ts
git commit -m "feat(start-workout): apply next weekly routine suggestion"
```

---

## 검증 체크리스트

- [ ] 주간 루틴 기본값은 `가슴 / 등 / 하체 / 팔+어깨` 4개 세션으로 시작한다
- [ ] 사용자가 저장한 주간 루틴 세션 템플릿이 앱 재시작 후에도 유지된다
- [ ] `팔+어깨` 루틴은 같은 완료 세션 안에 `arms`, `shoulders`가 모두 있을 때만 완료 처리된다
- [ ] 세부 부위가 지정된 루틴은 같은 완료 세션 안에 필요한 details가 모두 있어야 완료 처리된다
- [ ] Summary의 주간 루틴 카드는 완료/부분/예정 상태를 구분해서 표시한다
- [ ] 운동 시작 시 이번 주 미완료 루틴 세션이 기본 선택으로 반영된다
- [ ] 이번 주 루틴을 모두 완료하면 다음 추천은 `null`이고 새 기본 선택을 강제하지 않는다
