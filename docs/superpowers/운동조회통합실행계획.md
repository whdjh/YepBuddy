# 운동 조회 통합 실행 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 운동화면, 결과화면, 메인화면, 캘린더, 세션 목록을 `React Context + AsyncStorage + HealthKit` 계약 위에서 순차적으로 완성하고, 기능별 브랜치 5개로 안전하게 분리한다.

**Architecture:** `feat/workout-screen`이 세션 생성과 저장의 원천이다. `feat/result-screen`, `feat/main-screen`, `feat/calendar`, `feat/session-list`는 동일한 `sessionId`와 저장 포맷을 소비하는 구조로 구현한다. UI 스텁은 이미 존재하므로 이번 작업의 핵심은 공통 데이터 계약, 영속화, 조회 훅, 라우팅 연결이다.

**Tech Stack:** Expo Router, React Native, React Context, AsyncStorage, HealthKit, NativeWind, TypeScript

---

## 파일 구조 및 책임

### 공통 계약

**Files:**
- Create: `src/entities/workout-session/model/types.ts`
- Create: `src/entities/workout-session/model/sessionStorage.ts`
- Create: `src/entities/workout-session/api/healthKit.ts`
- Modify: `src/features/do-workout/index.ts`
- Modify: `src/features/view-result/index.ts`
- Modify: `src/features/view-summary/index.ts`
- Modify: `src/features/view-calendar/index.ts`

**책임:**
- `types.ts`: `BodyPart`, 저장용 세션 메타 타입, 결과화면/메인화면/캘린더 공용 타입 정의
- `sessionStorage.ts`: 현재 세션 저장, 완료 세션 저장, 세션 단건/목록 조회, 메모 수정 API
- `healthKit.ts`: 운동 저장, 운동 상세 조회, 월/주 단위 workout 조회를 감싸는 엔티티 API
- 각 feature `index.ts`: public API만 노출

**공통 계약:**
- `sessionId = startedAt ISO string`
- 현재 세션 키: `yb:workout:current`
- 완료 세션 키: `yb:workout:session:${sessionId}`
- 날짜 인덱스 키: `yb:workout:date:${YYYY-MM-DD}`
- 하루 1세션 기준으로 날짜와 `sessionId`는 1:1 매핑

**완료 세션 저장 형태:**

```ts
export type BodyPart =
  | "chest"
  | "back"
  | "lower"
  | "shoulder"
  | "arm"
  | "core"

export interface WorkoutBodyPartSet {
  part: BodyPart
  setCount: number
}

export interface WorkoutLocation {
  lat: number
  lng: number
}

export interface StoredWorkoutSession {
  sessionId: string
  startedAt: string
  completedAt: string
  bodyParts: WorkoutBodyPartSet[]
  memo: string
  location: WorkoutLocation | null
}
```

### 브랜치 전략

**브랜치 순서:**
1. `feat/workout-screen`
2. `feat/result-screen`
3. `feat/main-screen`
4. `feat/calendar`
5. `feat/session-list`

**병합 규칙:**
- `feat/result-screen`는 `feat/workout-screen`이 머지된 뒤 시작
- `feat/main-screen`는 `feat/result-screen`까지 머지된 뒤 시작
- `feat/calendar`는 `feat/result-screen`까지 머지된 뒤 시작 가능
- `feat/session-list`는 `feat/result-screen`까지 머지된 뒤 시작 가능
- 공통 계약을 임의로 변경할 경우 앞 브랜치로 돌아가 수정 후 재병합

---

### Task 1: `feat/workout-screen` 공통 세션 계약 확정

**Files:**
- Create: `src/entities/workout-session/model/types.ts`
- Create: `src/entities/workout-session/model/sessionStorage.ts`
- Create: `src/features/do-workout/model/types.ts`
- Create: `src/features/do-workout/model/workoutReducer.ts`
- Create: `src/features/do-workout/model/persistence.ts`
- Create: `src/features/do-workout/model/restore.ts`
- Create: `src/features/do-workout/model/WorkoutContext.tsx`
- Create: `src/features/do-workout/lib/sessionId.ts`
- Modify: `src/app/_layout.tsx`
- Modify: `src/features/do-workout/index.ts`

- [ ] **Step 1: 현재 구조 확인**

Run: `rg -n "do-workout|view-result|view-summary|view-calendar|start-workout" src`
Expected: 기존 스텁 화면과 export 구조를 확인하고, `do-workout/model`이 아직 비어 있음을 확인

- [ ] **Step 2: 세션 타입과 저장 키를 정의**

```ts
export const CURRENT_WORKOUT_STORAGE_KEY = "yb:workout:current"

export const getWorkoutSessionStorageKey = (sessionId: string) =>
  `yb:workout:session:${sessionId}`

export const getWorkoutDateStorageKey = (dateKey: string) =>
  `yb:workout:date:${dateKey}`
```

- [ ] **Step 3: `sessionId` 생성 함수를 추가**

```ts
export function createWorkoutSessionId(startedAt: string) {
  return startedAt
}
```

- [ ] **Step 4: workout reducer와 state를 정의**

```ts
export type WorkoutPhase =
  | "idle"
  | "countdown"
  | "recording"
  | "paused"
  | "completed"

export interface WorkoutState {
  phase: WorkoutPhase
  sessionId: string | null
  startedAt: string | null
  completedAt: string | null
  pausedAt: string | null
  pausedDuration: number
  bodyParts: WorkoutBodyPartSet[]
  memo: string
  location: WorkoutLocation | null
  heartRate: number | null
  activeKcal: number
  totalKcal: number
}
```

- [ ] **Step 5: 현재 세션 저장/복원 API를 구현**

```ts
export async function saveCurrentWorkoutSession(state: WorkoutState) {}
export async function loadCurrentWorkoutSession() {}
export async function clearCurrentWorkoutSession() {}
export async function saveCompletedWorkoutSession(
  session: StoredWorkoutSession,
) {}
export async function getStoredWorkoutSession(sessionId: string) {}
export async function updateStoredWorkoutMemo(sessionId: string, memo: string) {}
```

- [ ] **Step 6: Context provider를 앱 루트에 연결**

Run: `sed -n '1,220p' src/app/_layout.tsx`
Expected: 루트 레이아웃에서 `WorkoutProvider`를 감쌀 위치를 확인

- [ ] **Step 7: lint로 타입/경로 오류 확인**

Run: `bun run lint`
Expected: 새 model 레이어 추가 후 import/export 오류가 없거나, 후속 UI 연결 전까지 수정할 범위를 확인

- [ ] **Step 8: 커밋**

```bash
git checkout -b feat/workout-screen
git add src/entities/workout-session src/features/do-workout src/app/_layout.tsx
git commit -m "feat: add workout session contract and persistence"
```

---

### Task 2: `feat/workout-screen` 카운트다운/운동중/종료 플로우 연결

**Files:**
- Modify: `src/app/workout/countdown.tsx`
- Modify: `src/app/workout/active.tsx`
- Modify: `src/features/start-workout/ui/CountdownScreen.tsx`
- Modify: `src/features/do-workout/ui/ActiveWorkoutScreen.tsx`
- Modify: `src/features/do-workout/ui/WorkoutDrawer.tsx`
- Modify: `src/features/do-workout/ui/StatsSection.tsx`
- Modify: `src/features/do-workout/ui/BodyPartSelector.tsx`
- Modify: `src/features/do-workout/ui/SetCountList.tsx`
- Modify: `src/features/do-workout/ui/MemoSection.tsx`
- Create: `src/features/do-workout/lib/useWorkoutTimer.ts`
- Create: `src/features/do-workout/lib/useLocationOnce.ts`
- Create: `src/features/do-workout/lib/useHealthKitWorkout.ts`

- [ ] **Step 1: 카운트다운 종료 시 recording 상태로 전환**

```ts
const startedAt = new Date().toISOString()
const sessionId = createWorkoutSessionId(startedAt)
dispatch({ type: "START_RECORDING", payload: { startedAt, sessionId } })
router.replace("/workout/active")
```

- [ ] **Step 2: 운동중 화면의 mock state를 Context state로 교체**

```ts
const { state, toggleBodyPart, updateSetCount, updateMemo, pauseWorkout, resumeWorkout, completeWorkout } =
  useWorkout()
```

- [ ] **Step 3: 타이머 훅을 `startedAt`, `pausedAt`, `pausedDuration` 기반으로 구현**

```ts
const elapsedMs = now - startedAtMs - pausedDurationMs - pausedSegmentMs
```

- [ ] **Step 4: 위치 1회 기록은 실패 허용으로 구현**

```ts
try {
  const location = await getCurrentPositionAsync({})
  dispatch({ type: "SET_LOCATION", payload: location })
} catch {
  // 위치 권한 거부는 세션 진행을 막지 않음
}
```

- [ ] **Step 5: 운동 종료 시 완료 세션 저장 후 결과화면 이동**

```ts
const sessionId = state.sessionId
await saveCompletedWorkoutSession({
  sessionId,
  startedAt: state.startedAt!,
  completedAt,
  bodyParts: state.bodyParts,
  memo: state.memo,
  location: state.location,
})
await clearCurrentWorkoutSession()
router.replace(`/workout/${encodeURIComponent(sessionId)}`)
```

- [ ] **Step 6: recording/paused 세션 복원 로직을 앱 시작 시 실행**

```ts
if (snapshot?.phase === "recording" || snapshot?.phase === "paused") {
  dispatch({ type: "HYDRATE", payload: snapshot })
}
```

- [ ] **Step 7: lint로 화면 연결 검증**

Run: `bun run lint`
Expected: 카운트다운, active 화면, context 연결이 타입 오류 없이 통과

- [ ] **Step 8: 커밋**

```bash
git add src/app/workout src/features/start-workout src/features/do-workout
git commit -m "feat: connect workout flow and persistence"
```

---

### Task 3: `feat/result-screen` 세션 상세 조회와 메모 수정

**Files:**
- Modify: `src/app/workout/[id].tsx`
- Create: `src/features/view-result/model/useSessionDetail.ts`
- Create: `src/features/view-result/model/useUpdateMemo.ts`
- Create: `src/features/view-result/ui/MemoEditor.tsx`
- Modify: `src/features/view-result/ui/ResultScreen.tsx`
- Modify: `src/features/view-result/index.ts`
- Modify: `src/entities/workout-session/model/sessionStorage.ts`
- Modify: `src/entities/workout-session/api/healthKit.ts`

- [ ] **Step 1: 라우트 파라미터에서 `sessionId`를 읽도록 연결**

```ts
const { id } = useLocalSearchParams<{ id: string }>()
return <ResultScreen sessionId={decodeURIComponent(id)} />
```

- [ ] **Step 2: HK + AsyncStorage 병합 조회 훅을 추가**

```ts
const stored = await getStoredWorkoutSession(sessionId)
const hk = await getWorkoutDetail(sessionId)
return { stored, hk }
```

- [ ] **Step 3: 결과 화면의 mock 데이터를 실제 훅 데이터로 교체**

```ts
const { data, isLoading } = useSessionDetail(sessionId)
```

- [ ] **Step 4: 메모 수정은 AsyncStorage에만 반영**

```ts
await updateStoredWorkoutMemo(sessionId, memo)
```

- [ ] **Step 5: HK 없음 / 메타 없음 / 위치 없음 상태를 분기**

```ts
const durationLabel = data.hk?.duration ? formatDuration(data.hk.duration) : "--"
```

- [ ] **Step 6: lint로 결과화면 검증**

Run: `bun run lint`
Expected: 결과화면이 mock 없이 sessionId 기반으로 렌더링되고 타입 오류가 없음

- [ ] **Step 7: 커밋**

```bash
git checkout -b feat/result-screen
git add src/app/workout/[id].tsx src/features/view-result src/entities/workout-session
git commit -m "feat: load workout result by session id"
```

---

### Task 4: `feat/main-screen` 요약 데이터 연결

**Files:**
- Modify: `src/app/(tabs)/index.tsx`
- Create: `src/features/view-summary/model/useTodaySummary.ts`
- Create: `src/features/view-summary/model/useThisWeekSessions.ts`
- Create: `src/features/view-summary/model/useLatestSession.ts`
- Create: `src/features/view-summary/model/useTodayCompleted.ts`
- Modify: `src/features/view-summary/ui/SummaryScreen.tsx`
- Modify: `src/features/view-summary/ui/TodayWorkoutCard.tsx`
- Modify: `src/features/view-summary/ui/SessionLinkCard.tsx`
- Modify: `src/features/view-summary/ui/WorkoutLinkCard.tsx`
- Modify: `src/features/view-summary/ui/WeeklySessionList.tsx`

- [ ] **Step 1: 오늘 완료 세션 여부 훅을 구현**

```ts
export function useTodayCompleted() {
  // 오늘 날짜 키로 완료 세션 존재 여부 조회
}
```

- [ ] **Step 2: 최근 세션과 주간 세션 훅을 구현**

```ts
const latestSession = await getLatestStoredWorkoutSession()
const weekSessions = await getStoredWorkoutSessionsInRange(from, to)
```

- [ ] **Step 3: 오늘 요약 HK + 로컬 메타 병합 로직 추가**

```ts
const totalSets = stored.bodyParts.reduce((sum, item) => sum + item.setCount, 0)
const totalDuration = hkWorkouts.reduce((sum, workout) => sum + workout.duration, 0)
```

- [ ] **Step 4: 운동 시작 버튼을 완료 여부에 연결**

```ts
<WorkoutLinkCard disabled={todayCompleted} onPress={() => router.push("/workout/countdown")} />
```

- [ ] **Step 5: 최근 세션 카드와 주간 세션 리스트를 결과화면으로 연결**

```ts
router.push(`/workout/${encodeURIComponent(sessionId)}`)
```

- [ ] **Step 6: lint로 메인화면 검증**

Run: `bun run lint`
Expected: mock 데이터가 실제 훅으로 교체되고 빈 상태 분기가 타입 오류 없이 통과

- [ ] **Step 7: 커밋**

```bash
git checkout -b feat/main-screen
git add 'src/app/(tabs)/index.tsx' src/features/view-summary
git commit -m "feat: connect summary screen to workout sessions"
```

---

### Task 5: `feat/calendar` 월별 운동 날짜 및 결과화면 이동

**Files:**
- Modify: `src/app/calendar.tsx`
- Create: `src/features/view-calendar/model/types.ts`
- Create: `src/features/view-calendar/model/useMonthWorkoutDates.ts`
- Create: `src/features/view-calendar/model/useInfiniteMonths.ts`
- Modify: `src/features/view-calendar/ui/CalendarScreen.tsx`
- Modify: `src/features/view-calendar/ui/MonthGrid.tsx`
- Modify: `src/features/view-calendar/ui/DayCell.tsx`
- Modify: `src/features/view-calendar/ui/DayRing.tsx`
- Modify: `src/features/view-calendar/index.ts`

- [ ] **Step 1: 월 단위 workout 조회 훅을 구현**

```ts
export interface MonthWorkoutDates {
  workoutDates: Record<string, string>
}
```

- [ ] **Step 2: 날짜 키를 `YYYY-MM-DD`로 고정**

```ts
const dateKey = `${year}-${String(month).padStart(2, "0")}-${String(day).padStart(2, "0")}`
```

- [ ] **Step 3: mock 날짜 맵을 실제 훅 데이터로 교체**

```ts
const { workoutDates } = useMonthWorkoutDates(year, month)
```

- [ ] **Step 4: 운동한 날짜만 결과화면으로 이동**

```ts
const sessionId = workoutDates[dateKey]
if (sessionId) {
  router.push(`/workout/${encodeURIComponent(sessionId)}`)
}
```

- [ ] **Step 5: 미래 날짜 / HK 권한 없음 상태를 유지**

```ts
const isDisabled = isFuture || !sessionId
```

- [ ] **Step 6: lint로 캘린더 검증**

Run: `bun run lint`
Expected: 월별 렌더링과 날짜 클릭 라우팅이 타입 오류 없이 통과

- [ ] **Step 7: 커밋**

```bash
git checkout -b feat/calendar
git add src/app/calendar.tsx src/features/view-calendar
git commit -m "feat: connect calendar screen to workout sessions"
```

---

### Task 6: `feat/session-list` 세션 목록 화면 실제 데이터 연동

**Files:**
- Modify: `src/app/sessions.tsx`
- Create: `src/features/view-sessions/model/useSessionsByMonth.ts`
- Create: `src/features/view-sessions/model/useInfiniteSessions.ts`
- Create: `src/features/view-sessions/model/filterSessions.ts`
- Create: `src/features/view-sessions/model/types.ts`
- Modify: `src/features/view-sessions/ui/SessionListScreen.tsx`
- Modify: `src/features/view-sessions/ui/SessionCard.tsx`
- Create: `src/features/view-sessions/ui/FilterTabs.tsx`
- Modify: `src/features/view-sessions/index.ts`
- Modify: `src/entities/workout-session/api/healthKit.ts`
- Modify: `src/entities/workout-session/model/sessionStorage.ts`

- [ ] **Step 1: 현재 세션 목록 스텁 구조 확인**

Run: `sed -n '1,260p' src/features/view-sessions/ui/SessionListScreen.tsx`
Expected: `MOCK_SESSIONS` 기반 스텁, 월 그룹 UI, 필터 상태, 결과화면 이동 방식을 확인

- [ ] **Step 2: 세션 목록 화면 전용 타입 정의**

```ts
export interface SessionListItem {
  sessionId: string
  startedAt: string
  completedAt: string | null
  bodyParts: BodyPart[]
  totalSets: number
  kcal: number | null
}
```

Expected: 세션 목록 UI가 엔티티 원본 타입이 아니라 화면용 view model을 기준으로 동작

- [ ] **Step 3: 월 단위 세션 로더 구현**

```ts
const hkWorkouts = await getWorkoutSummariesForMonth(year, month)
const storedSessions = await getStoredWorkoutSessionsInRange(startDateKey, endDateKey)
```

Expected: 월 단위 HealthKit 조회와 로컬 메타 조회가 같은 범위에서 수행됨

- [ ] **Step 4: HealthKit workout과 AsyncStorage 메타 병합 로직 구현**

```ts
return hkWorkouts.map((workout) => {
  const sessionId = workout.startDate
  const stored = storedSessionMap.get(sessionId) ?? null
  return toSessionListItem(workout, stored)
})
```

Expected: 결과 화면 / 메인화면과 동일하게 `sessionId = startedAt ISO string` 계약을 기준으로 병합

- [ ] **Step 5: 무한 스크롤 상태 훅 구현**

```ts
const { months, loadMore, isLoadingMore } = useInfiniteSessions()
```

Expected: 현재 월부터 시작해서 과거 월을 순차적으로 추가 로드 가능

- [ ] **Step 6: 부위 필터 로직 구현**

```ts
const filteredSessions =
  activeFilter === "all"
    ? sessions
    : sessions.filter((session) => session.bodyParts.includes(activeFilter))
```

Expected: 로드된 세션 목록에 대해 클라이언트 사이드 필터가 적용되고, 부위 미선택 세션은 `"all"`에서만 노출

- [ ] **Step 7: 세션 카드 클릭 시 결과 화면 이동 연결**

```ts
router.push(`/workout/${encodeURIComponent(session.sessionId)}`)
```

Expected: `/sessions` 화면도 결과 화면과 동일한 `sessionId` 계약 사용

- [ ] **Step 8: 필터 탭 UI를 분리하고 기존 화면에 연결**

```tsx
<FilterTabs value={activeFilter} onChange={setActiveFilter} />
```

Expected: `모두 / 가슴 / 등 / 하체 / 어깨 / 팔 / 코어` 필터가 화면 책임과 분리됨

- [ ] **Step 9: 빈 리스트 / 필터 결과 없음 상태 처리**

```ts
if (filteredSessions.length === 0) {
  // 빈 상태 메시지 렌더링
}
```

Expected: 전체 세션 없음 상태와 필터 결과 없음 상태가 깨지지 않음

- [ ] **Step 10: lint로 세션 목록 화면 검증**

Run: `bun run lint`
Expected: `view-sessions`, `healthKit`, `sessionStorage` 변경 후 타입/경로 오류 없음

- [ ] **Step 11: 커밋**

```bash
git checkout -b feat/session-list
git add src/app/sessions.tsx src/features/view-sessions src/entities/workout-session
git commit -m "feat: connect session list screen to workout sessions"
```

---

### Task 7: 병합 및 최종 검증

**Files:**
- Modify: 필요 시 브랜치 충돌 해결 파일

- [ ] **Step 1: 브랜치 병합 순서대로 rebase 또는 merge**

Run: `git log --oneline --decorate --graph --all -20`
Expected: `feat/workout-screen` 이후 순서대로 브랜치가 이어지는지 확인

- [ ] **Step 2: 최종 lint 실행**

Run: `bun run lint`
Expected: 전체 코드베이스 lint 통과

- [ ] **Step 3: 수동 검증 체크리스트 수행**

```text
1. 메인화면에서 운동 시작 버튼 탭 → 카운트다운 → 운동중 화면 진입
2. 운동중 부위/세트/메모 입력 후 종료 → 결과화면 이동
3. 결과화면 새로 진입 시 동일 세션 메모가 유지
4. 메인화면 최근 세션/주간 세션 카드에서 결과화면 이동
5. 캘린더 운동 날짜 탭 시 결과화면 이동
```

- [ ] **Step 4: 병합 완료 커밋 또는 PR 준비**

```bash
git status
```

Expected: 의도한 변경만 남아 있고, 기능 브랜치 병합 준비 완료

---

## 사전 확인 사항

- `React Context`를 상태 관리 기준으로 사용한다.
- 백엔드는 사용하지 않는다. 운동 플로우는 `HealthKit + AsyncStorage + expo-*` 조합으로 처리한다.
- 실제 구현 전, 이 문서를 기준으로 공통 세션 계약이 충분히 명확한지 사용자 검토를 반드시 받는다.

## 셀프 리뷰

- Placeholder 없음: `TODO`, `TBD`, “나중에” 같은 미완성 표현 제거
- 내부 일관성 확인: 모든 화면이 같은 `sessionId` 계약과 storage 키 규약을 사용
- 범위 확인: 이번 문서는 운동/조회 5개 기능만 다루고, 알림/프로틴/위젯은 제외
- 모호성 제거: 메인화면, 캘린더, 세션 목록은 결과화면을 소비하는 조회 기능으로 고정
