# 7. 프로틴 페이지

<aside>
⚙️

- **탭:** 프로틴
- **역할:** 프로틴 제품의 가격 분석 및 g당 최저가 비교. 읽기 전용 클라이언트.
- **데이터 아키텍처:**
    - **데이터 원본:** 별도 크론 프로젝트에서 5분 주기로 프로틴 가격 추적 및 Supabase DB 갱신.
    - **앱 역할:** 읽기 전용(read-only) 클라이언트. `@supabase/supabase-js`로 직접 조회.
    - **보안:** Supabase RLS(Row Level Security) 활성화, `anon` 키에 `SELECT`만 허용.
- **정렬/가격 등급:** 서버에서 결정. 클라이언트는 관여하지 않는다.
</aside>

## 7.1. 화면 구성

### 타이틀

- "프로틴" / 부제: "가격 분석 · g당 최저가를 찾아보세요"

### ① 카테고리 필터

- **전체 | WPC | WPI | Blend**
- 단일 선택

### ② 프로틴 제품 리스트

- 각 카드: 제품명 + 가격 등급 뱃지(저점/중간/고점)
- 정보: 카테고리, 용량(g), 가격(원), g당 가격(원)
- **클릭 →** 8. 프로틴 상세 페이지

## 7.2. 가격 뱃지 로직

<aside>
⚙️

최근 180일 가격 데이터를 기반으로 백분위(Percentile)를 계산하여 서버에서 판정한다.

</aside>

| 뱃지 | 조건 | 색상 |
|------|------|------|
| 저점 | 하위 20% 이하 | Green |
| 중간 | 평균 범위 | Blue |
| 고점 | 상위 20% 이상 | Red |

## 7.3. 데이터 페칭

```typescript
useQuery({
  queryKey: ['proteins', category, search],
  queryFn: fetchProteins,
  staleTime: 1000 * 60 * 5,        // 5분간 캐시 유효
  gcTime: 1000 * 60 * 30,           // 30분간 가비지 컬렉션 유예
  retry: 3,
  retryDelay: (attempt) => Math.min(1000 * 2 ** attempt, 10000),
});
```

- 카테고리 필터 변경 시 `queryKey`가 변경되어 자동으로 재조회된다.
- 5분 이내 재진입 시 캐시된 데이터를 즉시 표시한다.

## 7.4. 엣지케이스

1. **네트워크 오류**
    - 3회 재시도 후에도 실패하면 에러 상태를 표시한다.
    - 이전에 캐시된 데이터가 있으면 캐시 데이터를 표시한다.
2. **제품이 0건인 카테고리**
    - 빈 리스트를 표시한다.

## 7.5. 기술 구현

### 라이브러리

| 역할 | 라이브러리 |
|------|-----------|
| DB 클라이언트 | `@supabase/supabase-js` |
| 데이터 페칭 | `@tanstack/react-query` |

---

## 이력 관리
ui: 2026-04-06